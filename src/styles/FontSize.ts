import { moderateScale } from '@/utils/SizeMatter';

const FONT_SIZE = {
    xxxl: moderateScale(20, 0.3), //20
    xxl: moderateScale(18, 0.3), //18
    xl: moderateScale(16, 0.3), //16
    lg: moderateScale(14, 0.3), //14
    md: moderateScale(12, 0.3), //12
    sm: moderateScale(10, 0.3), //10
};

export default FONT_SIZE;
